<header class="bg-white border-b-2 flex justify-around py-4 text-sm font-semibold leading-6 mb-4">
  <div class="flex justify-around flex gap-x-6">
    <div><%= link_to 'All Offers', offers_path %></div>
    <div><%= link_to 'Claimed Offers', claimed_offers_path %></div>
  </div>

  <div class="flex gap-x-6">
    <div>Hello, <%= current_player.name %></div>
    <%= button_to destroy_player_session_path, method: :delete, class: "text-red-500" do %>
      Log out &rarr;</span>
    <% end %>
  </div>
</header>
